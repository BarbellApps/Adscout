{
  "name": "adscout-scraper-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Isolated worker for broader Meta ad competitor coverage beyond the official Graph API. Deliberately NOT part of the Next.js app's deploy — see README.md before running.",
  "scripts": {
    "start": "node src/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "dotenv": "^17.4.2",
    "playwright": "^1.61.1"
  }
}
